interface:
  display_name: "Movie Resource Search"
  short_description: "Search titles and summarize provider availability"
  default_prompt: "Use $movie-search-and-download to identify the right movie or TV result and summarize available resource providers safely."
policy:
  allow_implicit_invocation: true
